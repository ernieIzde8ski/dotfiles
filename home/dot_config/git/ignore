.rgignore

# cache directories
# these tend to already contain a `.gitignore` with `.*`,
# but then they just show up in IDEs as empty directories
.ruff_cache
.mypy_cache
.venv
venv
*_venv

# editor configs
.nvim.lua
.vscode
